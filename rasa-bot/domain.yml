version: "3.1"

intents:
  - greet
  - goodbye
  - ask_ollama
  - affirm

responses:
  utter_greet:
    - text: "Hey! How can I help?"
  utter_goodbye:
    - text: "Goodbye 👋"
  utter_ask_clarify:
    - text: "I didn't understand — can you rephrase?"

slots:
  last_user_message:
    type: text
    influence_conversation: false
    mappings:
      - type: from_text  # captures raw user message

actions:
  - action_query_ollama

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
